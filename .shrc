#
# .shrc:
#       Common part of .bashrc, .zshrc, etc.
#

stty -ixon
stty status '^T' 2>/dev/null

if which dircolors >/dev/null 2>&1 && [ -f ${HOME}/.dircolors ]; then
    eval $(dircolors -b ${HOME}/.dircolors)
fi

. ${HOME}/.gnupg/gpg-agent.sh
