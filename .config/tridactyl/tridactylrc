# :installnative is required before using this

colors dark

bind gd tabdetach

bind a tabprev
bind s tabnext
bind z back
bind x forward

bind / fillcmdline find
bind ? fillcmdline find -?
bind n findnext 1
bind N findnext -1
